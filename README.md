# WIP - Will add lab or dev script, setups, playbooks, images and so on that will be used in my lab environment only

Run
~~~
ansible-playbook setup-rhel-hypervisor/setup_hv_rhel10.yaml -i "HOSTNAME," -u USER --ask-pass --ask-become-pass
~~~
